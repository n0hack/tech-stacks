import React from "react";
import ReactDOM from "react-dom/client";
import TutorialAlready from "./TutorialAlready";
import TutorialVerify from "./TutorialVerify";

const root = ReactDOM.createRoot(document.getElementById("root") as HTMLElement);
root.render(
  <React.StrictMode>
    <TutorialAlready />
  </React.StrictMode>
);
